package task;

import actor.locale.SimulationView;

public class ApplicationMain {
    public static void main(String[] args) {
        final int WIDTH = 700;
        final int HEIGHT = WIDTH;
        new SimulationView(WIDTH, HEIGHT);
    }
}
